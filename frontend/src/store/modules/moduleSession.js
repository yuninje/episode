// Session 정보와 관련된 Vuex Store Module
import axios from "axios";

export default {
  namespaced: true,
  state: {},
  getters: {
    // (state, getters, rootState, rootGetters)
  },
  mutations: {
    // (state, payload, rootState?)
  },
  actions: {
    // ({ dispatch, commit, getters, rootGetters }, data, ...)
  }
};
